```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```

This corrected query uses the `>=` operator, ensuring that employees with a salary of 100000 are also included in the results.